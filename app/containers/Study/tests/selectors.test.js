// import { fromJS } from 'immutable';
// import { makeSelectStudyDomain } from '../selectors';

// const selector = makeSelectStudyDomain();

describe('makeSelectStudyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
